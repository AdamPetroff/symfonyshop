{% extends ':admin:layout.html.twig' %}
{% block content %}

    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {{ form(loginform) }}

    <a href="{{ path('admin_forgotten_password') }}">Forgotten password</a>
{% endblock %}